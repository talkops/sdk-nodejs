# TalkOps Extension: <%= extension.name %>
<% if (extension.dockerRepository && (extension.dockerRepository.match(/\//g) || []).length === 1) { -%>
![Docker Pulls](https://img.shields.io/docker/pulls/<%= extension.dockerRepository %>)
<% } -%>

A TalkOps Extension made to work with [TalkOps](https://talkops.app).

**TalkOps enables real-time voice interaction with applications.**

<% if (extension.features.length) { -%>
## Features

<% for (feature of extension.features) { -%>
* <%= feature.trim() %>
<% } -%>
<% } -%>

## Installation

1. Sign in to [TalkOps](https://talkops.app).
2. Search for the `<%= extension.name %>` extension.
3. Add the extension and follow the instructions.
